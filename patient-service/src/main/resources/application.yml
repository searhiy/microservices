server:
  port: 8761
spring:
  application:
    name: eureka-server
  boot:
    admin:
      context-path: /admin
# standalone instance
#eureka:
#  instance:
#    hostname: localhost
#  client:
#    registerWithEureka: false
#    fetchRegistry: false
#    serviceUrl:
#      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
management:
  security:
    enabled: false